# Replace this file with the output of:
#   nixos-generate-config --show-hardware-config
# on the nikos workstation, then run:
#   sudo nixos-install --flake /etc/nixos/nikos#nikos

{ ... }:
{
  # PLACEHOLDER — generate with nixos-generate-config on the target machine
}
